function Doctype(props: Props) {
  // Note: the body of the function is just a placeholder,
  // it will be overridden by the jsx parser
  return null;
}

type Props = {
  attributes?: string;
};

export default Doctype;
